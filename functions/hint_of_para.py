# Type Hints for Parameters

# Type hints help indicate the expected types of parameters and return values

def greet(name: str, age: int) -> str:
    return f"Hello, {name}. You are {age} years old."

print(greet("Alice", 25))
